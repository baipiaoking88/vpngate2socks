services:
  vpngate2socks:
    build: .
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun
    ports:
      - "127.0.0.1:1080:1080"
    environment:
      - COUNTRY=${COUNTRY:-}
      - IP_TYPE=${IP_TYPE:-}
      - PROXY_PORT=1080
    restart: unless-stopped
